# Universal Agent Engine

Engine doctrine — response style, token efficiency, orchestration structure, project-scope variables — lives in [.claude/UAE.md](.claude/UAE.md) and is imported below. This layout is identical to what `install.sh` / `install.ps1` produce in a host codebase, so the engine repo dogfoods its own installed form.

@.claude/UAE.md

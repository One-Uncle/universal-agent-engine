# Shell scripts must check out with LF — CRLF breaks sh even in Git Bash.
*.sh text eol=lf

# PowerShell is happy either way; keep native Windows endings.
*.ps1 text eol=crlf

# Everything else: normalize on commit, platform-native on checkout.
* text=auto
